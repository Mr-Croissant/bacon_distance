Reasons why I chose SQL:

Ordered DB:
The data i need to store is very ordered (movie name, actor name, etc)

Optimizations with DFS:
Each level of DFS can be executed with a JOIN statement in SQL, SQL has optimization mechanisms, 
ensuring that the most critical part of the code is efficient

Eash to search:
because the data is ordered, and there is a lot of it, then SQL can optimize searches which is also intensive action